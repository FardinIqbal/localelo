<!DOCTYPE html>
<html lang="en">
<head>
  <title>LocalElo</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="description" content="LocalElo - The simple & powerful ranking system for gyms and sports clubs">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <!-- Tailwind CSS -->
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

  <!-- Google Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com"/>
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>
  <link
    href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Outfit:wght@400;500;600;700&display=swap"
    rel="stylesheet"
    />

  <!-- FontAwesome Icons -->
  <link
    rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
    />

  <!-- Import JavaScript Dependencies -->
  <%= javascript_importmap_tags %>
</head>
<body class="min-h-screen flex flex-col font-['Inter'] bg-gradient-to-br from-slate-950 via-slate-900 to-slate-950 text-slate-50">
<style>
    #mobile-menu {
        transition: opacity 0.3s ease, transform 0.3s ease;
        opacity: 1;
        transform: translateY(0);
    }

    #mobile-menu.hidden {
        opacity: 0;
        transform: translateY(-10px);
        pointer-events: none;
    }
</style>

<!-- Desktop Navigation Bar -->
<!-- Desktop Navigation Bar -->
<nav class="bg-slate-900/90 backdrop-blur-xl border-b border-white/10 sticky top-0 z-30 transition-all duration-300">
  <div class="container mx-auto px-4 py-3 flex justify-between items-center">
    <!-- Logo -->
    <%= link_to(
          user_signed_in? ? authenticated_root_path : unauthenticated_root_path,
          class: 'text-2xl font-bold tracking-tight flex items-center cursor-pointer'
        ) do %>
        <span class="flex items-center bg-gradient-to-r from-pink-500 to-indigo-500 bg-clip-text text-transparent transition-all duration-300">
          <i class="fas fa-chart-line mr-2"></i>
          LocalElo
        </span>
    <% end %>

    <!-- Desktop Navigation Links -->
    <div class="hidden md:flex items-center space-x-2">
      <% if user_signed_in? %>
        <%= link_to(
              authenticated_root_path,
              class: "px-3 py-2 rounded-md text-sm font-medium transition-all duration-300 relative text-gray-400 hover:text-indigo-500 nav-link #{'font-medium text-indigo-500' if current_page?(authenticated_root_path)}"
            ) do %>
          <i class="fas fa-home mr-1.5"></i>
          Home
        <% end %>

        <%= link_to(
              organizations_path,
              class: "px-3 py-2 rounded-md text-sm font-medium transition-all duration-300 relative text-gray-400 hover:text-indigo-500 nav-link #{'font-medium text-indigo-500' if current_page?(organizations_path)}"
            ) do %>
          <i class="fas fa-search mr-1.5"></i>
          Explore
        <% end %>

        <%= link_to(
              account_organizations_path,
              class: "px-3 py-2 rounded-md text-sm font-medium transition-all duration-300 relative text-gray-400 hover:text-indigo-500 nav-link #{'font-medium text-indigo-500' if current_page?(account_organizations_path)}"
            ) do %>
          <i class="fas fa-users mr-1.5"></i>
          My Organizations
        <% end %>


        <%= link_to(
              matches_path,
              class: "px-3 py-2 rounded-md text-sm font-medium transition-all duration-300 relative text-gray-400 hover:text-indigo-500 nav-link #{'font-medium text-indigo-500' if current_page?(matches_path)}"
            ) do %>
          <i class="fas fa-trophy mr-1.5"></i>
          Matches
        <% end %>

        <%= link_to(
              new_match_path,
              class: "inline-flex items-center justify-center font-medium rounded-md transition-all duration-300 h-10 px-4 text-white bg-gradient-to-r from-purple-600 to-pink-500 hover:bg-gradient-to-r hover:from-purple-500 hover:to-pink-400 shadow-lg shadow-purple-500/20 hover:shadow-purple-500/40 transform hover:-translate-y-1 relative overflow-hidden"
            ) do %>
          <i class="fas fa-plus mr-1.5"></i>
          Log Match
        <% end %>

        <!-- Profile Dropdown -->
        <div class="relative group ml-2">
          <button class="px-3 py-2 rounded-md text-sm font-medium transition-all duration-300 relative text-gray-400 hover:text-indigo-500 flex items-center">
            <div class="w-8 h-8 bg-slate-700 rounded-full flex items-center justify-center mr-2 border border-white/10">
              <i class="fas fa-user text-white/70"></i>
            </div>
            <span><%= current_user.username || 'Profile' %></span>
            <i class="fas fa-chevron-down ml-1.5 text-xs"></i>
          </button>
          <div class="absolute right-0 mt-2 w-48 bg-slate-800/90 backdrop-blur-xl rounded-lg shadow-lg overflow-hidden border border-white/10 opacity-0 invisible group-hover:opacity-100 group-hover:visible transform group-hover:translate-y-0 -translate-y-2 transition-all duration-300 z-50">
            <%= link_to(
                  user_path(current_user),
                  class: "block px-4 py-2 text-sm w-full text-left transition-colors duration-300 text-white hover:bg-slate-700/50 hover:text-pink-500"
                ) do %>
              <i class="fas fa-user mr-2"></i>
              My Profile
            <% end %>
            <%= link_to(
                  '#',
                  class: "block px-4 py-2 text-sm w-full text-left transition-colors duration-300 text-white hover:bg-slate-700/50 hover:text-pink-500"
                ) do %>
              <i class="fas fa-cog mr-2"></i>
              Settings
            <% end %>
            <div class="my-1 border-t border-white/5"></div>
            <%= button_to(
                  destroy_user_session_path,
                  method: :delete,
                  class: "block px-4 py-2 text-sm w-full text-left transition-colors duration-300 text-red-400 hover:bg-slate-700/50 hover:text-red-300"
                ) do %>
              <i class="fas fa-sign-out-alt mr-2"></i>
              Logout
            <% end %>
          </div>
        </div>
      <% else %>
        <%= link_to(
              organizations_path,
              class: "hidden md:inline-block px-3 py-2 rounded-md text-sm font-medium transition-all duration-300 relative text-gray-400 hover:text-indigo-500"
            ) do %>
          <i class="fas fa-search mr-1.5"></i>
          Explore
        <% end %>
        <%= link_to(
              new_user_session_path,
              class: "px-3 py-2 rounded-md text-sm font-medium transition-all duration-300 relative text-gray-400 hover:text-indigo-500"
            ) do %>
          <i class="fas fa-sign-in-alt mr-1.5"></i>
          Sign In
        <% end %>
        <%= link_to(
              new_user_registration_path,
              class: "inline-flex items-center justify-center font-medium rounded-md transition-all duration-300 h-10 px-4 text-white bg-gradient-to-r from-indigo-600 to-purple-600 hover:bg-gradient-to-r hover:from-indigo-500 hover:to-purple-500 shadow-lg shadow-indigo-500/20 hover:shadow-indigo-500/40 transform hover:-translate-y-1"
            ) do %>
          <i class="fas fa-user-plus mr-1.5"></i>
          Sign Up
        <% end %>
      <% end %>
    </div>

    <!-- Mobile Navigation -->
    <div class="flex md:hidden items-center space-x-2">
      <% if user_signed_in? %>
        <!-- Mobile Menu Button (only for signed-in users) -->
        <button
          id="mobile-menu-button"
          class="flex items-center justify-center w-10 h-10 rounded-full bg-slate-800 text-white border border-white/10"
        >
          <i class="fas fa-bars"></i>
        </button>
      <% else %>
        <!-- Always visible Sign In and Sign Up buttons for mobile -->
        <%= link_to(
              new_user_session_path,
              class: "inline-flex items-center justify-center font-medium rounded-md transition-all duration-300 h-9 px-3 text-gray-200 bg-slate-800/80 hover:bg-slate-700/80 border border-white/10 shadow-md"
            ) do %>
          <i class="fas fa-sign-in-alt mr-1.5"></i>
          Sign In
        <% end %>
        <%= link_to(
              new_user_registration_path,
              class: "inline-flex items-center justify-center font-medium rounded-md transition-all duration-300 h-9 px-3 text-white bg-gradient-to-r from-indigo-600 to-purple-600 hover:bg-gradient-to-r hover:from-indigo-500 hover:to-purple-500 shadow-lg shadow-indigo-500/20 hover:shadow-indigo-500/40"
            ) do %>
          <i class="fas fa-user-plus mr-1.5"></i>
          Sign Up
        <% end %>

      <% end %>
    </div>
  </div>

  <!-- Mobile Menu (only for signed-in users) -->
  <% if user_signed_in? %>
    <div id="mobile-menu" class="hidden md:hidden bg-slate-800/95 backdrop-blur-xl border-t border-white/10 transition-all duration-300">
      <div class="px-2 pt-2 pb-3 space-y-1">
        <%= link_to(
              authenticated_root_path,
              class: "block px-3 py-2 rounded-md text-base font-medium transition-all duration-300 #{'bg-slate-700/50 text-white' if current_page?(authenticated_root_path)} #{'text-gray-400 hover:bg-slate-700/30 hover:text-white' unless current_page?(authenticated_root_path)}"
            ) do %>
          <i class="fas fa-home mr-2"></i>
          Home
        <% end %>

        <%= link_to(
              organizations_path,
              class: "block px-3 py-2 rounded-md text-base font-medium transition-all duration-300 #{'bg-slate-700/50 text-white' if current_page?(organizations_path)} #{'text-gray-400 hover:bg-slate-700/30 hover:text-white' unless current_page?(organizations_path)}"
            ) do %>
          <i class="fas fa-search mr-2"></i>
          Explore
        <% end %>

        <%= link_to(
              account_organizations_path,
              class: "block px-3 py-2 rounded-md text-base font-medium transition-all duration-300 #{'bg-slate-700/50 text-white' if current_page?(account_organizations_path)} #{'text-gray-400 hover:bg-slate-700/30 hover:text-white' unless current_page?(account_organizations_path)}"
            ) do %>
          <i class="fas fa-users mr-2"></i>
          My Organizations
        <% end %>

        <%= link_to(
              matches_path,
              class: "block px-3 py-2 rounded-md text-base font-medium transition-all duration-300 #{'bg-slate-700/50 text-white' if current_page?(matches_path)} #{'text-gray-400 hover:bg-slate-700/30 hover:text-white' unless current_page?(matches_path)}"
            ) do %>
          <i class="fas fa-trophy mr-2"></i>
          Matches
        <% end %>

        <%= link_to(
              new_match_path,
              class: "block px-3 py-2 rounded-md text-base font-medium transition-all duration-300 text-white bg-gradient-to-r from-purple-600 to-pink-500 my-2"
            ) do %>
          <i class="fas fa-plus mr-2"></i>
          Log Match
        <% end %>

        <div class="border-t border-white/10 my-2 pt-2">
          <%= link_to(
                user_path(current_user),
                class: "block px-3 py-2 rounded-md text-base font-medium transition-all duration-300 text-gray-400 hover:bg-slate-700/30 hover:text-white"
              ) do %>
            <i class="fas fa-user mr-2"></i>
            My Profile
          <% end %>

          <%= link_to(
                '#',
                class: "block px-3 py-2 rounded-md text-base font-medium transition-all duration-300 text-gray-400 hover:bg-slate-700/30 hover:text-white"
              ) do %>
            <i class="fas fa-cog mr-2"></i>
            Settings
          <% end %>

          <%= button_to(
                destroy_user_session_path,
                method: :delete,
                class: "w-full text-left block px-3 py-2 rounded-md text-base font-medium transition-all duration-300 text-red-400 hover:bg-slate-700/30 hover:text-red-300"
              ) do %>
            <i class="fas fa-sign-out-alt mr-2"></i>
            Logout
          <% end %>
        </div>
      </div>
    </div>
  <% end %>
</nav>

<!-- Mobile Side Navigation -->
<div
  id="mobile-menu"
  class="fixed inset-0 z-50 transition-all duration-300 bg-black bg-opacity-50 backdrop-blur-sm hidden"
>
  <div
    class="h-full w-72 bg-slate-800/90 backdrop-blur-xl p-6 shadow-lg rounded-r-lg border-l border-white/10 transform -translate-x-full transition-transform duration-300"
    id="mobile-menu-container"
  >
    <div class="flex justify-between items-center mb-6">
      <div
        class="text-xl font-bold tracking-tight flex items-center cursor-pointer bg-gradient-to-r from-pink-500 to-indigo-500 bg-clip-text text-transparent transition-all duration-300"
      >
        <i class="fas fa-chart-line mr-2"></i>
        LocalElo
      </div>
      <button
        id="close-mobile-menu"
        class="w-10 h-10 flex items-center justify-center rounded-full bg-slate-700 text-white border border-white/10"
      >
        <i class="fas fa-times"></i>
      </button>
    </div>
    <% if user_signed_in? %>
      <div class="flex items-center mb-6 pb-4 border-b border-white/5">
        <div
          class="bg-slate-700 rounded-full w-12 h-12 flex items-center justify-center border border-white/10"
        >
          <i class="fas fa-user text-white/70 text-xl"></i>
        </div>
        <div class="ml-3">
          <div class="font-medium text-white"><%= current_user.username || 'User' %></div>
          <div class="text-sm text-white/60">View Profile</div>
        </div>
      </div>

      <%= link_to(
            authenticated_root_path,
            class: "mobile-nav-item block py-3 px-4 text-white rounded-md transition-all duration-300 hover:bg-slate-700/50 hover:text-pink-500 mb-1 #{'bg-slate-700/50 text-pink-500' if current_page?(authenticated_root_path)}"
          ) do %>
        <i class="fas fa-home mr-3 w-6 text-center"></i>
        Home
      <% end %>

      <%= link_to(
            organizations_path,
            class: "mobile-nav-item block py-3 px-4 text-white rounded-md transition-all duration-300 hover:bg-slate-700/50 hover:text-pink-500 mb-1 #{'bg-slate-700/50 text-pink-500' if current_page?(organizations_path)}"
          ) do %>
        <i class="fas fa-search mr-3 w-6 text-center"></i>
        Explore
      <% end %>

      <%= link_to(
            account_organizations_path,
            class: "mobile-nav-item block py-3 px-4 text-white rounded-md transition-all duration-300 hover:bg-slate-700/50 hover:text-pink-500 mb-1 #{'bg-slate-700/50 text-pink-500' if current_page?(account_organizations_path)}"
          ) do %>
        <i class="fas fa-users mr-3 w-6 text-center"></i>
        My Organizations
      <% end %>


      <%= link_to(
            matches_path,
            class: "mobile-nav-item block py-3 px-4 text-white rounded-md transition-all duration-300 hover:bg-slate-700/50 hover:text-pink-500 mb-1 #{'bg-slate-700/50 text-pink-500' if current_page?(matches_path)}"
          ) do %>
        <i class="fas fa-trophy mr-3 w-6 text-center"></i>
        My Matches
      <% end %>

      <%= link_to(
            new_match_path,
            class: "block py-3 px-4 text-white rounded-md transition-all duration-300 hover:bg-slate-700/50 hover:text-pink-500 mb-1"
          ) do %>
        <i class="fas fa-plus mr-3 w-6 text-center"></i>
        Log Match
      <% end %>

      <div class="border-t border-white/5 my-4"></div>

      <%= link_to(
            '#',
            class: "block py-3 px-4 text-white rounded-md transition-all duration-300 hover:bg-slate-700/50 hover:text-pink-500 mb-1"
          ) do %>
        <i class="fas fa-cog mr-3 w-6 text-center"></i>
        Settings
      <% end %>

      <%= button_to(
            destroy_user_session_path,
            method: :delete,
            class: "block py-3 px-4 text-red-400 rounded-md transition-all duration-300 hover:bg-slate-700/50 hover:text-red-300 mb-1 w-full text-left"
          ) do %>
        <i class="fas fa-sign-out-alt mr-3 w-6 text-center"></i>
        Logout
      <% end %>
    <% else %>
      <%= link_to(
            organizations_path,
            class: "mobile-nav-item block py-3 px-4 text-white rounded-md transition-all duration-300 hover:bg-slate-700/50 hover:text-pink-500 mb-1"
          ) do %>
        <i class="fas fa-search mr-3 w-6 text-center"></i>
        Explore
      <% end %>

      <%= link_to(
            new_user_session_path,
            class: "mobile-nav-item block py-3 px-4 text-white rounded-md transition-all duration-300 hover:bg-slate-700/50 hover:text-pink-500 mb-1"
          ) do %>
        <i class="fas fa-sign-in-alt mr-3 w-6 text-center"></i>
        Sign In
      <% end %>

      <%= link_to(
            new_user_registration_path,
            class: "mobile-nav-item block py-3 px-4 font-medium text-pink-500 rounded-md transition-all duration-300 hover:bg-slate-700/50 mb-1"
          ) do %>
        <i class="fas fa-user-plus mr-3 w-6 text-center"></i>
        Sign Up
      <% end %>
    <% end %>
  </div>
</div>

<!-- Flash Messages -->
<div class="container mx-auto px-4 pt-4">
  <div id="flash-container">
    <% flash.each do |type, message| %>
      <% flash_class =
           case type
           when "notice" then "bg-indigo-900/50 text-indigo-300 border-l-4 border-indigo-500"
           when "alert" then "bg-red-900/50 text-red-300 border-l-4 border-red-500"
           when "success" then "bg-green-900/50 text-green-300 border-l-4 border-green-500"
           else
             "bg-slate-800/50 text-slate-300 border-l-4 border-slate-500"
           end
      %>
      <div
        class="rounded-xl p-4 mb-4 flex justify-between items-center shadow-lg max-w-2xl mx-auto backdrop-blur-md opacity-100 transform translate-y-0 transition-all duration-500 <%= flash_class %>"
        role="alert"
      >
        <div class="flex items-center">
          <% if type == "notice" %>
            <i class="fas fa-info-circle mr-3 text-indigo-400"></i>
          <% elsif type == "alert" %>
            <i class="fas fa-exclamation-circle mr-3 text-red-400"></i>
          <% elsif type == "success" %>
            <i class="fas fa-check-circle mr-3 text-green-400"></i>
          <% end %>
          <p><%= message %></p>
        </div>
        <button class="close-flash text-lg hover:text-white transition-colors">&times;</button>
      </div>
    <% end %>
  </div>
</div>

<!-- Main Content -->
<main
  class="container mx-auto px-4 flex-grow py-8 md:py-12 opacity-0 transform translate-y-4 transition-all duration-500 <%= 'pb-16' if user_signed_in? %>"
>
  <%= yield %>
</main>

<% if user_signed_in? %>
  <!-- Mobile Bottom Navigation -->
  <!-- Mobile Bottom Navigation -->
  <div
    class="md:hidden fixed bottom-0 left-0 right-0 bg-slate-900/80 backdrop-blur-xl z-40 shadow-lg h-16 rounded-t-2xl transition-transform duration-300 border-t border-white/10"
    id="bottom-nav"
  >
    <div class="flex justify-around items-center h-full">
      <%= link_to(
            authenticated_root_path,
            class:
              "flex flex-col items-center justify-center text-white/60 w-1/5 hover:text-pink-500
               #{'text-pink-500' if current_page?(authenticated_root_path)}"
          ) do %>
        <i class="fas fa-home text-lg mb-1"></i>
        <span class="text-xs font-medium">Home</span>
      <% end %>

      <%= link_to(
            organizations_path,
            class:
              "flex flex-col items-center justify-center text-white/60 w-1/5 hover:text-pink-500
               #{'text-pink-500' if current_page?(organizations_path)}"
          ) do %>
        <i class="fas fa-search text-lg mb-1"></i>
        <span class="text-xs font-medium">Explore</span>
      <% end %>

      <%= link_to(
            new_match_path,
            class:
              "flex items-center justify-center rounded-full shadow-lg text-white w-14 h-14 -mt-7
               bg-gradient-to-r from-purple-600 to-pink-500
               hover:from-purple-500 hover:to-pink-400
               shadow-purple-500/20 hover:shadow-purple-500/40
               transform hover:scale-105"
          ) do %>
        <i class="fas fa-plus"></i>
      <% end %>

      <%= link_to(
            account_organizations_path,
            class:
              "flex flex-col items-center justify-center text-white/60 w-1/5 hover:text-pink-500
               #{'text-pink-500' if current_page?(matches_path)}"
          ) do %>
        <i class="fas fa-users text-lg mb-1"></i>
        <span class="text-xs font-medium">My Orgs</span>
      <% end %>

      <%= link_to(
            user_path(current_user),
            class:
              "flex flex-col items-center justify-center text-white/60 w-1/5 hover:text-pink-500
               #{'text-pink-500' if current_page?(user_path(current_user))}"
          ) do %>
        <i class="fas fa-user text-lg mb-1"></i>
        <span class="text-xs font-medium">Profile</span>
      <% end %>
    </div>
  </div>

<% end %>

<!-- JavaScript -->
<script>
    document.addEventListener('DOMContentLoaded', function() {
        const mobileMenu = document.getElementById('mobile-menu');
        const mobileMenuContainer = document.getElementById('mobile-menu-container');
        const mobileMenuButton = document.getElementById('mobile-menu-button');
        const closeMenuButton = document.getElementById('close-mobile-menu');
        const mobileNavLinks = document.querySelectorAll('.mobile-nav-item, #mobile-menu-container a, #mobile-menu-container button');

        // Open mobile menu with smooth transition
        function openMobileMenu() {
            mobileMenu.classList.remove('hidden');
            mobileMenu.style.opacity = '0';
            mobileMenu.style.transform = 'translateY(-10px)';

            // Trigger reflow
            mobileMenu.offsetHeight;

            // Show menu with animation
            mobileMenu.style.opacity = '1';
            mobileMenu.style.transform = 'translateY(0)';

            // Toggle icon between bars and X
            const icon = mobileMenuButton.querySelector('i');
            if (icon.classList.contains('fa-bars')) {
                icon.classList.remove('fa-bars');
                icon.classList.add('fa-times');
            }
        }

        // Close mobile menu with smooth transition
        function closeMobileMenu() {
            mobileMenu.style.opacity = '0';
            mobileMenu.style.transform = 'translateY(-10px)';

            // Reset the icon to bars
            const icon = mobileMenuButton.querySelector('i');
            if (icon.classList.contains('fa-times')) {
                icon.classList.remove('fa-times');
                icon.classList.add('fa-bars');
            }

            // Hide after transition completes
            setTimeout(() => {
                mobileMenu.classList.add('hidden');
            }, 300);
        }

        // Toggle the mobile menu on hamburger click
        mobileMenuButton?.addEventListener('click', function(e) {
            e.stopPropagation(); // Prevent this click from immediately closing the menu
            if (mobileMenu.classList.contains('hidden')) {
                openMobileMenu();
            } else {
                closeMobileMenu();
            }
        });

        closeMenuButton?.addEventListener('click', closeMobileMenu);

        // Close menu when clicking outside container
        document.addEventListener('click', function(e) {
            if (mobileMenu && !mobileMenu.classList.contains('hidden')) {
                // Check if the click was outside the menu
                if (!mobileMenu.contains(e.target) && e.target !== mobileMenuButton) {
                    closeMobileMenu();
                }
            }
        });

        // Prevent clicks inside the menu from closing it
        mobileMenu?.addEventListener('click', function(e) {
            e.stopPropagation();
        });

        // Add this new code: Close menu when clicking any link inside the menu
        mobileNavLinks.forEach(link => {
            link.addEventListener('click', () => {
                closeMobileMenu();
            });
        });

        // Also add this to handle Turbo navigation events
        document.addEventListener('turbo:visit', () => {
            if (mobileMenu && !mobileMenu.classList.contains('hidden')) {
                closeMobileMenu();
            }
        });

        // Flash messages auto-dismiss and manual close
        const flashMessages = document.querySelectorAll('.flash-message');
        flashMessages.forEach(message => {
            setTimeout(() => {
                message.style.opacity = '0';
                setTimeout(() => {
                    message.remove();
                }, 500);
            }, 5000);

            const closeButton = message.querySelector('.close-flash');
            if (closeButton) {
                closeButton.addEventListener('click', () => {
                    message.style.opacity = '0';
                    setTimeout(() => {
                        message.remove();
                    }, 500);
                });
            }
        });

        // Hide bottom nav on scroll
        const bottomNav = document.getElementById('bottom-nav');
        if (bottomNav) {
            let lastScrollTop = 0;
            const scrollThreshold = 20;

            window.addEventListener(
                'scroll',
                () => {
                    const scrollTop = window.pageYOffset || document.documentElement.scrollTop;
                    if (Math.abs(lastScrollTop - scrollTop) <= scrollThreshold) return;
                    if (scrollTop > lastScrollTop && scrollTop > 100) {
                        bottomNav.classList.add('translate-y-full');
                    } else {
                        bottomNav.classList.remove('translate-y-full');
                    }
                    lastScrollTop = scrollTop <= 0 ? 0 : scrollTop;
                },
                { passive: true }
            );
        }

        // Navbar shadow on scroll
        const navbar = document.querySelector('nav');
        function handleNavbarScroll() {
            if (window.scrollY > 10) {
                navbar.classList.add('shadow-lg', 'shadow-purple-500/5');
            } else {
                navbar.classList.remove('shadow-lg', 'shadow-purple-500/5');
            }
        }
        window.addEventListener('scroll', handleNavbarScroll, { passive: true });

        // Link highlighting
        function setActiveLinks() {
            const currentPath = window.location.pathname;

            // Desktop nav links
            document.querySelectorAll('.nav-link').forEach(link => {
                const href = link.getAttribute('href');
                if (href === currentPath) {
                    link.classList.add('font-medium', 'text-indigo-500');
                } else {
                    link.classList.remove('font-medium', 'text-indigo-500');
                }
            });

            // Mobile nav items
            document.querySelectorAll('.mobile-nav-item').forEach(link => {
                const href = link.getAttribute('href');
                if (href === currentPath) {
                    link.classList.add('bg-slate-700/50', 'text-pink-500');
                } else {
                    link.classList.remove('bg-slate-700/50', 'text-pink-500');
                }
            });

            // Bottom nav
            document.querySelectorAll('.mobile-nav-button').forEach(btn => {
                const href = btn.getAttribute('href');
                if (href === currentPath) {
                    btn.classList.add('text-pink-500');
                } else {
                    btn.classList.remove('text-pink-500');
                }
            });
        }
        setActiveLinks();

        // Tooltips
        function initTooltips() {
            document.querySelectorAll('[data-tooltip]').forEach(el => {
                el.addEventListener('mouseenter', () => {
                    const tooltipText = el.getAttribute('data-tooltip');
                    const tooltip = document.createElement('div');
                    tooltip.className = 'absolute bg-slate-800 text-white text-xs rounded-md py-1 px-2 z-50 backdrop-blur-md border border-white/10 shadow-lg shadow-purple-500/10';
                    tooltip.style.bottom = 'calc(100% + 5px)';
                    tooltip.style.left = '50%';
                    tooltip.style.transform = 'translateX(-50%)';
                    tooltip.textContent = tooltipText;

                    const arrow = document.createElement('div');
                    arrow.className = 'absolute w-0 h-0';
                    arrow.style.borderLeft = '5px solid transparent';
                    arrow.style.borderRight = '5px solid transparent';
                    arrow.style.borderTop = '5px solid #1e293b';
                    arrow.style.bottom = '-5px';
                    arrow.style.left = '50%';
                    arrow.style.transform = 'translateX(-50%)';
                    tooltip.appendChild(arrow);

                    el.style.position = 'relative';
                    el.appendChild(tooltip);
                });
                el.addEventListener('mouseleave', () => {
                    const tooltip = el.querySelector('.bg-slate-800');
                    if (tooltip) tooltip.remove();
                });
            });
        }
        initTooltips();

        // Simple data-table clickable sort icons
        function initializeDataTables() {
            const tables = document.querySelectorAll('.data-table');
            tables.forEach(table => {
                const headers = table.querySelectorAll('th');
                headers.forEach(header => {
                    if (header.getAttribute('data-sortable') !== 'false') {
                        header.classList.add('cursor-pointer', 'select-none');
                        const iconSpan = document.createElement('span');
                        iconSpan.innerHTML = ' <i class="fas fa-sort text-white/40 ml-1"></i>';
                        header.appendChild(iconSpan);

                        header.addEventListener('click', () => {
                            const currentSort = header.getAttribute('data-sort') || 'none';
                            headers.forEach(h => {
                                h.setAttribute('data-sort', 'none');
                                const icon = h.querySelector('i');
                                if (icon) {
                                    icon.className = 'fas fa-sort text-white/40 ml-1';
                                }
                            });
                            let newSort = 'asc';
                            if (currentSort === 'asc') newSort = 'desc';

                            header.setAttribute('data-sort', newSort);
                            const icon = header.querySelector('i');
                            if (icon) {
                                icon.className = `fas fa-sort-${
                                    newSort === 'asc' ? 'up' : 'down'
                                } text-pink-500 ml-1`;
                            }
                            // Add your own sorting logic or server request here
                        });
                    }
                });
            });
        }
        initializeDataTables();

        // Page transitions with Turbo
        document.addEventListener('turbo:before-visit', () => {
            const mainContent = document.querySelector('main');
            if (mainContent) {
                mainContent.style.opacity = '0';
                mainContent.style.transform = 'translateY(10px)';
            }
        });

        document.addEventListener('turbo:load', () => {
            setActiveLinks();
            initTooltips();
            initializeDataTables();
            const mainContent = document.querySelector('main');
            if (mainContent) {
                mainContent.style.opacity = '1';
                mainContent.style.transform = 'translateY(0)';
                mainContent.style.transition = 'opacity 0.5s ease, transform 0.5s ease';
            }
        });

        // Toast notification system
        window.showToast = function(message, type = 'info') {
            const container = document.getElementById('toast-container');
            const toast = document.createElement('div');
            toast.className = 'flex items-center mb-3 pointer-events-auto bg-slate-800/90 backdrop-blur-md rounded-lg shadow-md p-4 max-w-sm w-full border-l-4 border-white/10';
            toast.style.opacity = '0';
            toast.style.transform = 'translateY(20px)';
            toast.style.transition = 'opacity 0.5s ease, transform 0.5s ease';

            let iconClass = 'fa-info-circle text-indigo-400';
            let borderColor = 'border-indigo-500';
            if (type === 'success') {
                iconClass = 'fa-check-circle text-green-400';
                borderColor = 'border-green-500';
            } else if (type === 'error') {
                iconClass = 'fa-exclamation-circle text-red-400';
                borderColor = 'border-red-500';
            } else if (type === 'warning') {
                iconClass = 'fa-exclamation-triangle text-yellow-400';
                borderColor = 'border-yellow-500';
            }

            toast.classList.add(borderColor);
            toast.innerHTML = `
        <i class="fas ${iconClass} mr-3"></i>
        <span class="flex-1 text-white">${message}</span>
        <button class="ml-2 text-white/60 hover:text-white transition-colors duration-300">
          <i class="fas fa-times"></i>
        </button>
    `;

            container.appendChild(toast);

            // Trigger reflow
            toast.offsetHeight;

            // Show toast
            toast.style.opacity = '1';
            toast.style.transform = 'translateY(0)';

            // Auto hide after 3 seconds
            setTimeout(() => {
                toast.style.opacity = '0';
                toast.style.transform = 'translateY(-20px)';
                setTimeout(() => {
                    toast.remove();
                }, 500);
            }, 3000);

            const closeBtn = toast.querySelector('button');
            closeBtn.addEventListener('click', () => {
                toast.style.opacity = '0';
                toast.style.transform = 'translateY(-20px)';
                setTimeout(() => {
                    toast.remove();
                }, 500);
            });
        };

        // Add CSS for mobile menu transitions
        const style = document.createElement('style');
        style.textContent = `
        #mobile-menu {
            transition: opacity 0.3s ease, transform 0.3s ease;
        }
    `;
        document.head.appendChild(style);
    });

</script>

<!-- Toast Container -->
<div
  class="fixed z-50 flex flex-col items-center pointer-events-none bottom-20 left-0 right-0"
  id="toast-container"
></div>



</body>
</html>
