<div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">
  <div class="space-y-2">
    <h2 class="text-2xl font-semibold text-slate-900">Welcome, <%= current_user.first_name || current_user.username %></h2>
    <p class="text-sm text-slate-500">Track matches, ratings, and memberships in one place.</p>
  </div>
  <div class="text-sm text-slate-500">
    <%= Time.current.strftime("%A, %B %d, %Y") %>
  </div>
</div>
